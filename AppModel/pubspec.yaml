name: voice_recognition_app
description: A Flutter app with voice recognition feature.

version: 1.0.0+1

environment:
  sdk: ">=2.19.6 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  speech_to_text: ^6.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
